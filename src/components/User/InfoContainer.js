import { Flex } from "@chakra-ui/react";
import React from "react";

const InfoContainer = ({ info }) => {
  return (
    <Flex bg="purple.300" p={2} borderRadius="7pt" width="95%" align="center">
      InfoContainer
    </Flex>
  );
};

export default InfoContainer;
